class Solution:
    def numberOfSubstrings(self, s: str) -> int:
        res=0
        n=len(s)

        ls=[-1,-1,-1]
        for i in range(n):
            ind=ord(s[i])-ord('a')
            ls[ind]=i
            res=res+(1+min(ls))

        return res
