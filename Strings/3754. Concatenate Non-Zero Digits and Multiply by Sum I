class Solution:
    def sumAndMultiply(self, n: int) -> int:
        x=''
        total=0
        if n==0:
            return 0
        num=str(n)
        for ch in num:
            if ch!='0':
                x=x+ch
                total+=int(ch)

        return int(x)*total
