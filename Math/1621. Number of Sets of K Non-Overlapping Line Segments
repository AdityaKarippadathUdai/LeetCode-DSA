class Solution:
    def numberOfSets(self, n: int, k: int) -> int:
        MOD = 10**9 + 7
        a = n + k - 1
        b = 2 * k

        res = 1

        for i in range(1, b + 1):
            res = res * (a - i + 1) // i

        return res % MOD
