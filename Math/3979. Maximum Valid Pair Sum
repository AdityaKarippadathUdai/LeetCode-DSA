class Solution:
    def maxValidPairSum(self, nums: list[int], k: int) -> int:
        n=len(nums)
        res=0
        ml=nums[0]
        for i in range(k,n):
            ml=max(ml,nums[i-k])
            res=max(res,ml+nums[i])

        return res
