class Solution:
    def isMiddleElementUnique(self, nums: list[int]) -> bool:
        n=len(nums)
        ele=nums[n//2]
        res=0
        for i in range(n):
            if nums[i]==ele:
                res+=1
            if res>1:
                return False
        return res==1
