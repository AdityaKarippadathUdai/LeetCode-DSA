class Solution:
    def findDuplicates(self, nums: List[int]) -> List[int]:
        res=[]

        for a in nums:
            idx=abs(a)-1

            if nums[idx]>=0:
                nums[idx]=-nums[idx]
            else:
                res.append(idx+1)

        return res
