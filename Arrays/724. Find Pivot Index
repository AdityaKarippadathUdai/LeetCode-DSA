class Solution:
    def pivotIndex(self, nums: List[int]) -> int:
        lsum=0
        rsum=sum(nums)
        n=len(nums)

        for i in range(n):
            rsum-=nums[i]
            if lsum==rsum:
                return i

            lsum+=nums[i]
        return -1
